- name: Vérifier si Ghostty est installé
  stat:
    path: "/Applications/Ghostty.app"
  register: ghostty_app

- name: Installer Ghostty via Homebrew Cask
  homebrew_cask:
    name: ghostty
    state: present
  when: not ghostty_app.stat.exists

- name: Créer le répertoire de config Ghostty
  file:
    path: "{{ home }}/Library/Application Support/com.mitchellh.ghostty"
    state: directory

- name: Supprimer la config Ghostty existante
  file:
    path: "{{ home }}/Library/Application Support/com.mitchellh.ghostty/config"
    state: absent

- name: Symlink config Ghostty → dotfiles
  file:
    src: "{{ dotfiles_path }}/.config/ghostty/config"
    dest: "{{ home }}/Library/Application Support/com.mitchellh.ghostty/config"
    state: link
    force: true
